# This is a basic workflow to help you get started with Actions 
# Test

on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2.3.2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        protocol: ftps
        server: access811905793.webspace-data.io
        username: u99623908-aure
        password: ${{ secrets.KRACK_FTP_KEY }}
        server-dir: /Studiokrack/wp-content/plugins/email-signature-generator/
        security: loose
